; Cấu hình cho board BW16 (Module RTL8720DN phổ biến nhất)
[env:bw16]
platform = libretiny
board = bw16
framework = arduino

; Tốc độ Monitor (nếu bạn dùng Remote Serial, nhưng trên Cloud thì dòng này để tham khảo)
monitor_speed = 115200

; Các thư viện cần thiết (Ví dụ)
lib_deps =
    ; Thêm thư viện nếu cần
    
